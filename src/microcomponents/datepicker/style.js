import style from 'provider/style';

const styles = (theme) => ({
  datepicker: {
    width: '100%',
    height: 48,
    color: theme.palette.text.primary,
    borderRadius: 5,
    // border: 'none',
  },
});

export default style(styles);
