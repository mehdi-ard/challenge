import style from 'provider/style';

const styles = (theme) => ({
  button: {
    // borderRadius: 5,
    // background: '#14cc8b',
    // height: 40,
    // color: theme.palette.text.contrastText,
    // border: 'none',
    '&:hover': {
      // background: '#14cc8b',
      // color: theme.palette.text.contrastText,
      // border: 'none',
    },
    '&:focus': {
      // background: '#14cc8b',
      // color: theme.palette.text.contrastText,
      // border: 'none',
    },
  },
});

export default style(styles);
