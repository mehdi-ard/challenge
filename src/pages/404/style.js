import style from 'provider/style';

const styles = (theme) => ({
  notFoundPage: {
    width: '100%',
    height: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fff',
  },
});

export default style(styles);
